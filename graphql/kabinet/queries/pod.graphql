query ListPod {
  pods {
    ...ListPod
  }
}

query GetPod($id: ID!) {
  pod(id: $id) {
    ...Pod
  }
}

query GetPodForAgent($clientId: ID!) {
  podForAgent(clientId: $clientId) {
    ...Pod
  }
}

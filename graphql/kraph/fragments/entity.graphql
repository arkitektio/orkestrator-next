fragment Entity on Entity {
  id
  label
  category {
    id
    label
  }
  subjectedTo {
    id
    performedAt
    name
  }
}

fragment ListEntity on Entity {
  id
  label
  category {
    id
    label
  }
}

fragment EntityGraphNode on Entity {
  id
  label
  category {
    color
  }
}

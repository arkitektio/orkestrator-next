fragment Expression on Expression {
  id
  label
  ontology {
    id
    name
  }
  description
  store {
    ...MediaStore
  }
}

fragment ListExpression on Expression {
  id
  label
  description
  store {
    ...MediaStore
  }
}

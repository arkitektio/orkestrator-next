fragment NodeQuery on NodeQuery {
  id
  graph {
    id
    name
  }
  name
  pinned
  query
  description
  columns {
    ...Column
  }
  kind
}

fragment ListNodeQuery on NodeQuery {
  id
  name
  query
  description
  pinned
}

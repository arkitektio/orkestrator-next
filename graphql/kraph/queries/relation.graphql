query GetRelation($id: ID!) {
  relation(id: $id) {
    ...Relation
  }
}

query SearchRelations($category: ID!, $values: [ID!]) {
  options: relations(
    relationCategoryId: $category,
    filters: { ids: $values }
    pagination: { limit: 10 }
  ) {
    value: id
    label: label
  }
}



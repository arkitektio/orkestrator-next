mutation from_file_like(
  $file: FileLike!
  $name: String!
  $dataset: ID
  $viewPagination: OffsetPaginationInput
) {
  fromFileLike(input: { file: $file, fileName: $name, dataset: $dataset }) {
    ...File
  }
}



mutation DeleteFile($id: ID!) {
  deleteFile(input: { id: $id })
}

mutation from_parquet_like(
  $dataframe: ParquetLike!
  $name: String!
  $origins: [ID!]
  $dataset: ID
) {
  fromParquetLike(
    input: {
      dataframe: $dataframe
      name: $name
      origins: $origins
      dataset: $dataset
    }
  ) {
    ...Table
  }
}


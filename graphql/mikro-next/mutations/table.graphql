mutation from_parquet_like(
  $dataframe: ParquetLike!
  $name: String!
  $dataset: ID
) {
  fromParquetLike(
    input: { dataframe: $dataframe, name: $name, dataset: $dataset }
  ) {
    ...Table
  }
}


query GetFile($id: ID!) {
  file(id: $id) {
    ...File
  }
}

query GetFiles(
  $filters: FileFilter
  $pagination: OffsetPaginationInput
  $order: FileOrder
) {
  files(filters: $filters, pagination: $pagination, order: $order) {
    ...ListFile
  }
}

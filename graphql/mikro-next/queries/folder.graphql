query GetFolder($id: ID!) {
  folder(id: $id) {
    ...Folder
  }
}

query GetFolders(
  $filters: FolderFilter
  $pagination: OffsetPaginationInput
  $ordering: [FolderOrder!]
) {
  folders(filters: $filters, pagination: $pagination, ordering: $ordering) {
    ...ListFolder
  }
}

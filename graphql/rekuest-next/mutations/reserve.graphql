mutation Reserve(
  $action: ID
  $hash: ActionHash
  $implementation: ID
  $title: String
) {
  reserve(
    input: {
      action: $action
      implementation: $implementation
      hash: $hash
      title: $title
    }
  ) {
    ...PostmanReservation
  }
}

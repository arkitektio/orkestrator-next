query Clients(
  $pagination: OffsetPaginationInput
  $filters: ClientFilter
  $ordering: [ClientOrder!]
) {
  clients(ordering: $ordering, pagination: $pagination, filters: $filters) {
    ...ListClient
  }
}

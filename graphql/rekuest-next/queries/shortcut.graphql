query Shortcuts(
  $pagination: OffsetPaginationInput
  $filters: ShortcutFilter
  $ordering: [ShortcutOrder!]
) {
  shortcuts(ordering: $ordering, pagination: $pagination, filters: $filters) {
    ...ListShortcut
  }
}

query Shortcut($id: ID!) {
  shortcut(id: $id) {
    ...Shortcut
  }
}

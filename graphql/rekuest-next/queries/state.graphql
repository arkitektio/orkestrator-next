query GetState($id: ID!){
    state(id: $id){
        ...State
    }
}

query GetStateFor($template: ID, $agent: ID, $stateKey: String!){
    stateFor(template: $template, agent: $agent, stateKey: $stateKey){
        ...State
    }
}
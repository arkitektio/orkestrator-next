query GetState($id: ID!) {
  state(id: $id) {
    ...State
  }
}

query GetStateFor($implementation: ID, $agent: ID, $stateHash: String!) {
  stateFor(implementation: $implementation, agent: $agent, stateHash: $stateHash) {
    ...State
  }
}

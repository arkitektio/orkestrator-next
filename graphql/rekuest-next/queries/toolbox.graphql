query Toolboxes(
  $pagination: OffsetPaginationInput
  $filters: ToolboxFilter
  $ordering: [ToolboxOrder!]
) {
  toolboxes(ordering: $ordering, pagination: $pagination, filters: $filters) {
    ...ListToolbox
  }
}

query Toolbox($id: ID!) {
  toolbox(id: $id) {
    ...Toolbox
  }
}
